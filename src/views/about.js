const about = {
    render: () => `
    <template id="template-route">
    <h2>About</h2>
    </template>`
}

//The line below brings about to global scope!
// window.about = about

export default about